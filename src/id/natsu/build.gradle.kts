plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Natsu"
    className = "Natsu"
    versionCode = 32
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "natsuid"
    baseUrl = "https://natsu.tv"
}
