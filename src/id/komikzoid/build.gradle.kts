plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Komikzoid"
    className = "Komikzoid"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "colorlibanime"
    baseUrl = "https://komikzoid.id"
}
