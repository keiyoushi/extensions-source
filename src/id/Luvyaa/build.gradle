ext {
    extName = 'Luvyaa'
    extClass = '.Luvyaa'
    extVersionCode = 1
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

android {
    defaultConfig {
        manifestPlaceholders += [
            SOURCEHOST: "luvyaa.my.id",
            SOURCESCHEME: "https"
        ]
    }
}

dependencies {
    implementation(project(':lib-multisrc:mangathemesia'))
}