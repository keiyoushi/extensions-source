plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Izanami Scans"
    className = "IzanamiScans"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://izanamiscans.my.id"
}
