plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Stick Horse"
    versionCode = 0
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "madara"

    source {
        lang = "es"
        baseUrl = "https://stickhorse.cl"
    }
}
