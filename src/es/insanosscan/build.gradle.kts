plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "InsanosScan"
    className = "InsanosScan"
    versionCode = 31
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
}
