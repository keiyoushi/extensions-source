plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Rncalation"
    versionCode = 60
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"

    source {
        lang = "es"
        baseUrl = "https://rncalation.online"
    }
}
