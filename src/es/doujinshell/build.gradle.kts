plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "DoujinsHell"
    className = "DoujinsHell"
    versionCode = 1
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://doujinshell.net"
}
