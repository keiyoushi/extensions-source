plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "VCPVMP"
    versionCode = 9
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "vercomics"

    source {
        lang = "es"
        baseUrl = "https://vercomicsporno.com"
    }
}
