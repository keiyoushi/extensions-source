plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Marmota"
    className = "Marmota"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://marmota.me"
}
