plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Bloom Scans"
    versionCode = 2
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "mangathemesia"

    source {
        lang = "es"
        baseUrl = "https://bloomscans.com"
    }
}
