plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Harem de Kira"
    versionCode = 3
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "madara"

    source {
        lang = "es"
        baseUrl = "https://kiraproject.lat"
        versionId = 2
    }
}
