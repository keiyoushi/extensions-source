plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "MangaLector"
    className = "Mangalector"
    versionCode = 0
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://mangalector.com"
}
