plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Raiki Scan"
    className = "RaikiScan"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://raikiscan.com"
}
