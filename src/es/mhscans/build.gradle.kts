plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "MHScans"
    className = "MHScans"
    versionCode = 14
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://mhscans.com"
}
