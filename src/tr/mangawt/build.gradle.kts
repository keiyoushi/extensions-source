plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "MangaWT"
    className = "MangaWT"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://mangawt.com"
}
