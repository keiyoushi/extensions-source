plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Tempest Scans"
    className = "TempestScans"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://tempestmangas.com"
}
