plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "DocTruyen5s"
    versionCode = 4
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "liliana"

    source {
        lang = "vi"
        baseUrl = "https://manga.io.vn"
    }
}
