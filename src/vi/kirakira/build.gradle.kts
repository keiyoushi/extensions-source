plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "KiraKira"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "vi"
        baseUrl = "https://truyenkira.net"
    }
}
