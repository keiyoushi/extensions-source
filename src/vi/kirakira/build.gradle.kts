plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "KiraKira"
    versionCode = 2
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "vi"
        baseUrl = "https://truyenkira.com"
    }
}
