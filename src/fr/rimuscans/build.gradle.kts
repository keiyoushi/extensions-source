plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Rimu Scans"
    versionCode = 35
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "fr"
        baseUrl = "https://rimuscan.fr"
    }
}
