plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Scan Reader"
    className = "ScanReader"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "scanreader"
    baseUrl = "https://scanreader.net"
}
