plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "FMTEAM"
    className = "FMTEAM"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "pizzareader"
    baseUrl = "https://fmteam.fr"
}
