plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Epsilon Scan"
    className = "EpsilonScan"
    versionCode = 52
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "pam"
    baseUrl = "https://epsilonscan.to"
}
