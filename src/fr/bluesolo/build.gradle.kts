plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Blue Solo"
    className = "BlueSolo"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "pizzareader"
    baseUrl = "https://bluesolo.org"
}
