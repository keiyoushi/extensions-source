plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Kairos Toons"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "stalkercms"

    source {
        lang = "pt-BR"
        baseUrl = "https://kairostoons.net"
    }
}
