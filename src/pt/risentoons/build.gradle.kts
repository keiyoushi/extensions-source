plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Risentoons"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "stalkercms"

    source {
        lang = "pt-BR"
        baseUrl = "https://risentoons.xyz"
    }
}
