ext {
    extName = 'KuroMangas'
    extClass = '.KuroMangas'
    extVersionCode = 8
    isNsfw = false
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':lib:cookieinterceptor'))
}
