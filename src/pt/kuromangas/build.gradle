ext {
    extName = 'KuroMangas'
    extClass = '.KuroMangas'
    extVersionCode = 4
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:cookieinterceptor'))
}
