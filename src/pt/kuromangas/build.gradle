ext {
    extName = 'KuroMangas'
    extClass = '.KuroMangas'
    extVersionCode = 1
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:cookieinterceptor'))
}
