plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Monte Tai"
    className = "MonteTai"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://montetaiscanlator.xyz"
}
