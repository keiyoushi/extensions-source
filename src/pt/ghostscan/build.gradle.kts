plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Ghost Scan"
    className = "GhostScan"
    versionCode = 2
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://ghostscan.xyz"
}
