plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "RF Dragon Scan"
    className = "RFDragonScan"
    versionCode = 12
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
}
