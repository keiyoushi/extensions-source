plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Commit Strip"
    className = "CommitStripFactory"
    versionCode = 4
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
}
