ext {
    extName = 'Niadd'
    extClass = '.NiaddFactory' // O ponto no início é obrigatório para extensão
    extVersionCode = 1
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:cookieinterceptor"))
}
