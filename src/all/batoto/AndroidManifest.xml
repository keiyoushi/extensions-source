<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<application>
    <activity
        android:name=".all.batoto.UrlActivity"
        android:excludeFromRecents="true"
        android:exported="true"
        android:theme="@android:style/Theme.NoDisplay">
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />

            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />

            <data android:host="xbat.app" />
            <data android:host="xbat.si" />
            <data android:host="xbat.io" />
            <data android:host="xbat.me" />
            <data android:host="xbat.tv" />
            <data android:host="xbat.la" />

            <data
                android:pathPattern="/title/..*"
                android:scheme="https" />
        </intent-filter>
    </activity>
</application>
</manifest>
