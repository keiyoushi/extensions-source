ext {
    extName = 'Bato.to'
    extClass = '.BatoToFactory'
    extVersionCode = 50
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:cryptoaes'))
}
