plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "ComicsKingdom"
    className = "ComicsKingdomFactory"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
}
