plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Senkuro"
    className = "Senkuro"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "senkuro"
    baseUrl = "https://senkuro.com"
}
