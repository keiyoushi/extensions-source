plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Arya Scans"
    className = "AryaScans"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://brainrotcomics.com"
}
