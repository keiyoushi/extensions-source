plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Philia Scans"
    className = "PhiliaScans"
    versionCode = 58
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
}
