plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "TCB Scans"
    versionCode = 12
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "en"
        baseUrl = "https://tcbonepiecechapters.com"
    }
}
