plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Arc-Relight"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangadventure"

    source {
        lang = "en"
        baseUrl = "https://arc-relight.com"
    }
}
