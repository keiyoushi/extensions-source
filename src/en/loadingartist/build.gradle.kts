plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Loading Artist"
    className = "LoadingArtist"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
}
