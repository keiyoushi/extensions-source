plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Team Shadowi"
    versionCode = 1
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"

    source {
        lang = "en"
        baseUrl = "https://www.team-shadowi.com"
    }
}
