plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Eva Scans"
    className = "EvaScans"
    versionCode = 2
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://evascans.org"
}
