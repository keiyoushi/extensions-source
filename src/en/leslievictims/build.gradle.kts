plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Leslie&Victims"
    className = "LeslieAndVictims"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
}
