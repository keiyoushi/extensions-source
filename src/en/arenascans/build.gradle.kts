plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Arena Scans"
    versionCode = 0
    contentWarning = ContentWarning.MIXED
    libVersion = "1.4"
    theme = "mangathemesia"

    source {
        lang = "en"
        baseUrl = "https://arenascan.com"
    }
}
