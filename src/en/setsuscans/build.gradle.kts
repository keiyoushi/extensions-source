plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Setsu Scans"
    className = "SetsuScans"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://setsuscans.com"
}
