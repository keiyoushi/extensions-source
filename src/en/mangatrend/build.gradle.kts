plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Manga Trend"
    className = "MangaTrend"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://mangatrend.org"
}
