plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Manga Kiss"
    className = "MangaKiss"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://mangakiss.org"
}
