plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Rage Scans"
    className = "RageScans"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://ragescans.com"
}
