plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Ken Scans"
    className = "KenScans"
    versionCode = 10
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "iken"
    baseUrl = "https://kencomics.com"
}
