plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "WuxiaWorld"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"

    source {
        lang = "en"
        baseUrl = "https://wuxiaworld.site"
    }
}
