plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Lua Scans"
    className = "LuaScans"
    versionCode = 20
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "heancms"
    baseUrl = "https://luacomic.org"
}
