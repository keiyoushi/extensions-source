plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "XYZ Comics"
    className = ".XyzComics"
    versionCode = 5
    baseUrl = "https://xyzcomics.com"
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
}
