ext {
    set("extName", "Alternative Scans")
    set("extVersionCode", 1)
    set("overrideVersionCode", 0)
    set("extClass", ".AlternativeScans")
    set("baseUrl", "https://alternativescans.icu")
}

apply(from = "$rootDir/common.gradle")