ext {
    extName = 'Mangago'
    extClass = '.Mangago'
    extVersionCode = 32
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:cookieinterceptor'))
}
