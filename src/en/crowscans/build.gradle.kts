plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Crow Scans"
    className = "CrowScans"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://crowscans.xyz"
}
