ext {
    extName = 'Tapas'
    pkgNameSuffix = 'en.tapastic'
    extClass = '.Tapastic'
    extVersionCode = 20
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib-textinterceptor'))
}
