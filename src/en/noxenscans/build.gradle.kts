plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Noxen Scans"
    className = "NoxenScans"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://noxenscan.com"
}
