plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "SUPER MEGA"
    versionCode = 4
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "en"
        baseUrl = "https://www.supermegacomics.com"
    }
}
