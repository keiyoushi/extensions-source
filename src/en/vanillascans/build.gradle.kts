plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Vanilla Scans"
    className = "VanillaScans"
    versionCode = 0
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "iken"
    baseUrl = "https://vanillascans.org"
}
