plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Kewn Scans"
    className = "KewnScans"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "keyoapp"
    baseUrl = "https://kewnscans.org"
}
