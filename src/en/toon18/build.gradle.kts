plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Toon18"
    versionCode = 0
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "madara"

    source {
        lang = "en"
        baseUrl = "https://toon18.to"
    }
}
