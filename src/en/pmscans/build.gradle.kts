plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Rackus"
    className = "Rackus"
    versionCode = 7
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://rackusreads.com"
}
