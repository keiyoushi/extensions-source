plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "MurimScan"
    className = "MurimScan"
    versionCode = 36
    contentWarning = ContentWarning.NSFW
    libVersion = "1.4"
    theme = "zeistmanga"
    baseUrl = "https://www.murimscans.site"
}
