plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Firescans"
    className = "Firescans"
    versionCode = 4
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "madara"
    baseUrl = "https://firescans.xyz"
}
