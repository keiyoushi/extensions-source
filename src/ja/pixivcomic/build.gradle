ext {
    extName = 'Pixiv Comic'
    extClass = '.PixivComic'
    extVersionCode = 4
    nsfw = true
    libVersion = "1.6"
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(":lib:publus"))
}
