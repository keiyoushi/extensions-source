plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Young Animal"
    versionCode = 0
    contentWarning = ContentWarning.MIXED
    libVersion = "1.4"
    theme = "comiciviewer"

    source {
        lang = "ja"
        baseUrl = "https://younganimal.com"
    }
}
