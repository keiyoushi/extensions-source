plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "GANMA!"
    versionCode = 5
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "ja"
        baseUrl = "https://ganma.jp"
    }
}
