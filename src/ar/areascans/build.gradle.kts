plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Area Scans"
    className = "AreaScans"
    versionCode = 2
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mangathemesia"
    baseUrl = "https://ar.areascans.org"
}
