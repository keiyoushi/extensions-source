plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Manhatok"
    className = "Manhatok"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "zeistmanga"
    baseUrl = "https://manhatok.blogspot.com"
}
