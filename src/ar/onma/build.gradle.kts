plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Onma"
    versionCode = 3
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "mmrcms"

    source {
        name = "مانجا اون لاين"
        lang = "ar"
        baseUrl = "https://onma.me"
    }
}
