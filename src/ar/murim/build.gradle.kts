plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Murim"
    className = "Murim"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    theme = "zeistmanga"
    baseUrl = "https://www.murim.site"
}
