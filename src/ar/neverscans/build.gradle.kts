plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "NeverScans"
    className = "NeverScans"
    versionCode = 1
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"
    baseUrl = "https://neverscans.com"
}
