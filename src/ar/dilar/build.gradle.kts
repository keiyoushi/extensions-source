plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Dilar"
    versionCode = 8
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "ar"
        baseUrl = "https://dilar.tube"
        versionId = 2
    }
}
